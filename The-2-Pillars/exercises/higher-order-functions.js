const num1 = (num) => {
    return num;
} 

const num2 = (num) => {
    return num;
}

const multiply = (n1,n2) => {
    return console.log(`the result of ${num1(n1)} X ${num2(n2)} is ${n1*n2}`);
} 

multiply(5,8)